// Sum Numbers
function sum (numbers) {
  return numbers.reduce((acc, cur) => acc + cur, 0)
};

1. numbers 배열의 누적값을 구하면 됩니다.
2. reduce 메소드를 사용할 것 입니다.
3. 초기값을 0으로 설정하고 화살표 함수로 acc, cur (콜백함수, 최근값을 넣어줍니다.)
