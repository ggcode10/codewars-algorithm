function squareSum(numbers){
  let sum = 0;
  numbers.forEach(x => {
    sum += (x * x)
  })
  return sum
}

각각의 요소를 제곱해서 더하는 함수를 만들면 된다.

내가 사용한 방법은 forEach로 결과값을 담을 변수 sum을 선언 한 후
forEach로 각각의 요소를 x * x 형식으로 값을 구한뒤 sum에 넣었다.

forEach말고도 reducer, for loop로도 풀이가 가능하다.
