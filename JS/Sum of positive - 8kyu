const positiveSum = arr => arr.reduce((acc, cur) => cur > 0 ? acc + cur : acc, 0)

우선 화살표 함수를 사용합니다. 입력받은 배열의 누적값을 사용하기 위해 reduce를 사용합니다.
인자는 두개, acc와 cur입니다.
삼항연산자를 사용해서 cur이 0보다 크면 acc + cur을 작으면 acc를 반환하게 해줍니다.
