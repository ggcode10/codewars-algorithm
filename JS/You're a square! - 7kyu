const isSquare = function (n) {
    let sqrt = Math.sqrt(n)
    if (Number.isInteger(sqrt)) {
        return true;
    }
    return false
}
console.log(isSquare(4)) // true
conosle.log(isSquare(36)) // true

제곱근을 판별하는 문제

접근 방식
1. Math.sqrt는 숫자의 제곱근을 반환한다.
2. Number.isInteger()는 주어진 값이 정수인지 판별한다.
3. 그래서 조건문에 n의 제곱근을 반환한 변수 sqrt가 정수인지 확인
4. 정수이면 true, 아니면 false
